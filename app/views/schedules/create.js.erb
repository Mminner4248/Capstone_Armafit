<%# creates the json data that is used to populate the calendar. %>

$('.calendar').fullCalendar(
  'renderEvent', 
  $.parseJSON("<%=j render(@schedule, format: :json).html_safe %>"),
  true
);
$('.modal').modal('hide');