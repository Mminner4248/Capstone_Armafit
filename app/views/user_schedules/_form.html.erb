<%= form_with(model: user_schedule, local: true) do |form| %>
  <% if user_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_schedule.errors.count, "error") %> prohibited this user_schedule from being saved:</h2>

      <ul>
      <% user_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :schedule_id %>
    <%= form.text_field :schedule_id, id: :user_schedule_schedule_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_schedule_user_id %>
  </div>

  <div class="field">
    <%= form.label :user_notes %>
    <%= form.text_area :user_notes, id: :user_schedule_user_notes %>
  </div>

  <div class="field">
    <%= form.label :attended %>
    <%= form.check_box :attended, id: :user_schedule_attended %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
