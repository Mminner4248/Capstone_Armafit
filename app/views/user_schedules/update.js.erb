$('.calendar').fullCalendar('removeEvents', [<%= @user_schedule.id %>]);
$('.calendar').fullCalendar(
  'renderEvent', 
  $.parseJSON("<%=j render(@user_schedule, format: :json).html_safe %>"),
  true
);
$('.modal').modal('hide');